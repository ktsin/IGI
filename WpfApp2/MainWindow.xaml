<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="Лабораторная работа №2" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Label Content="Таблицы" HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Stretch" FontFamily="DejaVu Sans" FontSize="18"/>
            <ListBox VerticalAlignment="Stretch" DataContext="TableNames"/>
            <Button Content="Открыть БД" Margin="10, 20"/>
        </StackPanel>
        <DataGrid Grid.Column="1"/>
    </Grid>
</Window>
